custom:
  message: Hello from git

# spring:
#   datasource:
#     url: jdbc:mysql://${MYSQL_HOST}:3306/product_db
#     username: ${MYSQL_USER}
#     password: ${MYSQL_PASS}
spring:	
  datasource:	
    url: jdbc:mysql://ecommerce-db.c1o40sooi4jm.ap-south-1.rds.amazonaws.com:3306/product_db	
    username: admin	
    password: admin12345	
    driver-class-name: com.mysql.cj.jdbc.Driver	

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.springframework.boot.context.config: DEBUG
    org.springframework.core.env: DEBUG
